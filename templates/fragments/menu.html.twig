{#{% set currentPath = path(app.request.attributes.get('_route'),#}
{#    app.request.attributes.get('_route_params')) %}#}

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {% if currentPath is same as('/') %} active {% endif %}" aria-current="page" href="{{ path('home_page') }}">Liste des restaurants</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if currentPath is same as('/restaurateur/') %} active {% endif %}" href="{{ path('restaurateur_page_profil') }}">Espace restaurateur</a>
                </li>
            </ul>
        </div>
    </div>
</nav>