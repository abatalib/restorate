<div class="modal" tabindex="-1" role="dialog" id="confirmDeletePhotoModal">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Confirmation</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <h3 class="text-danger">Etes-vous sûr de supprimer cette photo ?</h3>
        <form method="post" class="d-inline" id="formDeletePhoto">
          <input type="hidden" name="_token" value="{{ csrf_token('delete.photo' ~ app.user.password) }}">
          <button class="btn btn-outline-danger mt-3">
            Supprimer
          </button>
        </form>
        <button class="btn btn-outline-secondary mt-3 btn-close-modal">
          Annuler
        </button>
      </div>
    </div>
  </div>
</div>